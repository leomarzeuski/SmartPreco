export enum ErrorEnum {
  NOT_FOUND = 'entity not found',
  INSERT = 'insert error',
  UPDATE = 'update error',
  DELETE = 'delete error',
  NO_FILE = 'no file provided',
  FORMAT_NOT_ALLOWED = 'only PNG and JPEG images are allowed',
  SIZE_EXCEEDS_LIMIT = 'image size exceeds the size limit',
  UNKNOWN_ERROR = 'unknown error',
}